<%= page_title('Settings','', 'heading3') %>

<%= render 'settings/settings_menu' %>
  <div class="submenucontent">
    <h3 class="heading4">All Owners</h3>
    <ul class="simple-list-with-text width-100" id="profiles">
      <%= turbo_frame_tag 'format-list' do %>
        <%= render @profiles %>
      <% end %>
    </ul>
