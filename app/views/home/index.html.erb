<%= page_title("Welcome to the UZW18 Books Database #{current_owner&.name}",'', 'heading2 color-accent-dark') %>
<% if current_owner %>
  Not you?
  <%= button_to 'Logout', destroy_owner_session_path, method: 'delete',
    class: 'nobutton color-accent-dark color-hover-accent-light' %>
<% else %>
  Want to register your books? Come and
  <%= link_to 'Log in', new_owner_session_path,
    class: 'color-accent-dark color-hover-accent-light' %> or
  <%= link_to 'Sign up', new_owner_registration_path,
    class: 'color-accent-dark color-hover-accent-light' %>
<% end %>
<h3 class="heading3 margin-top-1">All Genres</h3>
<div class="genre-cloud">
  <%= render CloudComponent.new(model: 'Genre') %>
</div>
<h3 class="heading3 margin-top-1">Latest Books</h3>
<ul class="itemlist">
  <% @books.each do |book| %>
    <%= render 'books/booklist_small', book: book %>
  <% end %>
</ul>

