  <%= simple_form_for shelf, html: { class: 'inline-form' } do |f| %>
    <%= f.input :name,
      placeholder: 'Shelf Name',
      label: false,
      wrapper_html: { class: 'width-100' }
    %>
    <%= f.button :submit, raw('&#x23ce'), class: 'inline-button' %>
  <% end %>
