  <%= simple_form_for genre, html: { class: 'title-form' } do |f| %>
    <%= f.input :name,
      placeholder: 'Genre Name',
      label: false
    %>
    <%= f.button :submit, raw('&#x23ce'), class: 'title-button' %>
  <% end %>

