<%= turbo_frame_tag 'page' do %>
  <%= turbo_stream_action_tag(
    'append',
    target: 'tag_books',
    template: %(#{render @books})
  ) %>
  <%= turbo_stream_action_tag(
    'replace',
    target: 'pager',
    template: %(#{render 'shared/pager', pagy: @pagy, model_path: genre_path(@genre, page: @pagy.next, show: 'list')})
    ) %>
<% end %>
