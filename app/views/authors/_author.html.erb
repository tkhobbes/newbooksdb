<%= case params[:show]
    when 'list' then render('authorlist', author: author)
    when 'unused' then render('authorsettings', author: author)
    else render('authorcard', author: author)
    end
%>
