  <%= simple_form_for book_format, html: { class: 'inline-form' } do |f| %>
    <%= f.input :name, placeholder: 'Bookformat Name', label: false %>
    <%= f.button :submit, raw('&#x23ce'), class: 'inline-button' %>
  <% end %>

