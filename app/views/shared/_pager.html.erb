<nav id="pager" class="pagy-nav pagination">
  <% if pagy.next %>
    <span class="page">
        <%= link_to(
          'Load more',
          books_path(page: pagy.next, show: params[:show]),
          data: {
            turbo_frame: 'page'
          }
        ) %>
    </span>
  <% end %>
</nav>
