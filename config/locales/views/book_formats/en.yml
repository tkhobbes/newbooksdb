en:
  BookFormats:
    book_format:
      default: " (default)"
      delete_name: books will be reassigned to the default book format
    new_form:
      link_title: Add new
    set_default:
      select: Select...
    index:
      title: Settings
      subtitle: Available
      default_title: Default
